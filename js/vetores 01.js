let numArray = []

function getArray(){
  for (n = 0;numArray.length < 10; n++){
    numArray[n] = Math.floor(Math.random() * 30)
    console.log(numArray)
  } 
}
  





  // 1) Preencher (ler) um vetor X de 10 elementos com o valor sorteado  inteiro de 1 até 30. Escrever o vetor X após seu total preenchimento. 